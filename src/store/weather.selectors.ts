import { createSelector } from '@ngrx/store';

import { WeatherState } from './weather.reducer';

// export const availableCities = (state: WeatherState) => state.availableCities;
// export const availableCities = createSelector((state: WeatherState) => state.availableCities,);

// export const cityByIndex = () => createSelector(
//   (state: WeatherState, props: {index: number}) => state.selectedCities[props.index],
//   (counter, props) => counter * props.multiply
// );
